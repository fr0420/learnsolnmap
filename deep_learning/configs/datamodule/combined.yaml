_target_: datamodules.datamodules.CombinedDataModule

supervised:
  train:
    data_dir: ${paths.data_dir}/train
    sequence_len: 6
    dtype: float32
    downsample_factor: 1.

  test:
    data_dir: ${paths.data_dir}/test
    sequence_len: 6
    dtype: float32
    downsample_factor: 1.
  
  batch_size: 100

unsupervised:
  train:
    data_dir: ${paths.data_dir}/train
    dtype: float32
    downsample_factor: 1.

  test:
    data_dir: ${paths.data_dir}/test
    dtype: float32
    downsample_factor: 1.

  batch_size: 500

num_workers: 4
pin_memory: True