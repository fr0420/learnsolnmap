i2h:
  _target_: torch.nn.Identity

h2h:
  _target_: networks.pirate.PirateNet
  input_dim: 12
  output_dim: 12
  hidden_dim: 500
  n_blocks: 2
  n_linears_per_block: 3
  activation:
    _target_: torch.nn.ELU

h2o:
  _target_: torch.nn.Identity